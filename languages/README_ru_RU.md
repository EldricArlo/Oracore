<div align="center">
  <img src="../src/media/icon-256.png" alt="Oracipher Icon" width="128">
  <h1 style="border-bottom: none;">Oracipher Core</h1>

# Библиотека ядра гибридного шифрования с высоким уровнем безопасности

| Сборка | Лицензия | Язык | Зависимости |
| :---: | :---: | :---: | :---: |
| ![Build Status](https://img.shields.io/badge/build-passing-brightgreen) | ![License](https://img.shields.io/badge/license-Dual--Licensed-blue) | ![Language](https://img.shields.io/badge/language-C11-purple) | ![Libsodium](https://img.shields.io/badge/libsodium-v1.0.18+-brightgreen) ![OpenSSL](https://img.shields.io/badge/OpenSSL-v3.0+-0075A8) ![Libcurl](https://img.shields.io/badge/libcurl-v7.68+-E5522D) |

</div>

## 1. 😎 Видение проекта и основные принципы

Этот проект представляет собой высокоуровневую библиотеку ядра гибридного шифрования, реализованную на стандарте C11 и ориентированную на безопасность. Его цель — предоставить проверенный в боевых условиях образец, демонстрирующий, как объединить ведущие в отрасли криптографические библиотеки (**libsodium**, **OpenSSL**, **libcurl**) в надежное, стабильное и простое в использовании решение для сквозного шифрования.

Наша разработка следует этим основным принципам безопасности:

*   🥸 **Выбор проверенной современной криптографии:** Никогда не реализовывать криптографические алгоритмы самостоятельно. Использовать только признанные сообществом современные криптографические примитивы, устойчивые к атакам по побочным каналам.
*   🤠 **Эшелонированная оборона:** Безопасность не должна зависеть от какого-либо одного уровня. Защита выстраивается на всех уровнях — от управления памятью и дизайна API до протоколов.
*   🙃 **Безопасные настройки по умолчанию и «отказ в доступе при сбое» (Fail-Closed):** Поведение системы по умолчанию должно быть безопасным. При возникновении неопределенного состояния (например, невозможности проверить статус отзыва сертификата) система должна выбрать отказ и прекратить операцию (Fail-Closed), а не продолжать выполнение.
*   🫥 **Минимизация раскрытия конфиденциальных данных:** Жизненный цикл, область видимости и время нахождения в памяти критически важных данных, таких как закрытые ключи, должны быть строго ограничены до абсолютного минимума.

## 2. 🥲 Ключевые особенности

*   😮 **Надежная модель гибридного шифрования:**
    *   **Симметричное шифрование:** Предоставляет поточное AEAD-шифрование на основе **XChaCha20-Poly1305** для больших блоков данных и однократное AEAD-шифрование для малых блоков данных.
    *   **Асимметричное шифрование:** Использует **X25519** (на основе Curve25519) для инкапсуляции симметричного сессионного ключа, гарантируя, что только предполагаемый получатель сможет его расшифровать.

*   🫨 **Современный стек криптографических примитивов:**
    *   **Производная ключа:** Используется **Argon2id**, победитель конкурса хеширования паролей, эффективно противостоящий взлому с помощью GPU и ASIC.
    *   **Цифровая подпись:** Используется **Ed25519**, обеспечивающий высокую скорость и надежность цифровых подписей.
    *   **Унификация ключей:** Умело используется свойство ключей Ed25519 безопасно преобразовываться в ключи X25519, что позволяет одной паре мастер-ключей удовлетворять потребности как в подписи, так и в шифровании.

*   😏 **Комплексная поддержка инфраструктуры открытых ключей (PKI):**
    *   **Жизненный цикл сертификата:** Поддерживается генерация запросов на подпись сертификата (CSR) в соответствии со стандартом X.509 v3.
    *   **Строгая проверка сертификатов:** Предоставляется стандартизированный процесс проверки сертификатов, включая цепочку доверия, срок действия и соответствие субъекта.
    *   **Обязательная проверка отзыва (OCSP):** Встроена строгая проверка по протоколу OCSP с политикой «отказа в доступе при сбое», немедленно прекращающей операцию, если статус сертификата не может быть подтвержден как действительный.

*   🧐 **Непробиваемая безопасность памяти:**
    *   Функции безопасной памяти `libsodium` доступны через публичный API, что позволяет клиентам безопасно обрабатывать конфиденциальные данные (например, сессионные ключи).
    *   Все внутренние закрытые ключи хранятся в заблокированной памяти, **что предотвращает их выгрузку на диск операционной системой**, и безопасно очищаются перед освобождением.

*   😵‍💫 **Высококачественные инженерные практики:**
    *   **Четкие границы API:** Предоставляется единый публичный заголовочный файл `hsc_kernel.h`, использующий непрозрачные указатели для инкапсуляции всех деталей внутренней реализации, что обеспечивает высокую связность и низкое зацепление.
    *   **Модульное тестирование:** Включает набор модульных тестов, покрывающих основные функции криптографии и PKI, обеспечивая корректность и надежность кода.
    *   **Полная документация и примеры:** Предоставляется подробный `README.md`, а также готовые к запуску демонстрационная программа и утилита командной строки.

## 3. 🤓 Структура проекта

Проект имеет четкую, многоуровневую структуру каталогов для разделения ответственности.

```
.
├── include/
│   └── hsc_kernel.h      # [ЯДРО] Единственный публичный заголовочный файл API
├── src/                  # Исходный код
│   ├── common/           # Общие внутренние модули (безопасная память, спецификации безопасности)
│   ├── core_crypto/      # Внутренние модули криптографии (обертка для libsodium)
│   ├── pki/              # Внутренние модули PKI (обертки для OpenSSL, libcurl)
│   ├── hsc_kernel.c      # [ЯДРО] Реализация публичного API
│   ├── main.c            # Пример использования API: Демонстрация сквозного процесса
│   └── cli.c             # Пример использования API: Мощная утилита командной строки
├── tests/                # Модульные тесты
│   ├── test_*.c          # Модульные тесты для различных модулей
│   └── test_helpers.h/.c # Вспомогательные функции для тестов
├── Makefile              # Скрипт для сборки и управления задачами
└── README.md             # Документация этого проекта
```

## 4. 🤥 Быстрый старт

### 4.1. Зависимости окружения

*   **Инструменты сборки:** `make`
*   **Компилятор C:** `gcc` или `clang` (с поддержкой стандарта C11)
*   **libsodium:** (`libsodium-dev`)
*   **OpenSSL:** Рекомендуется **v3.0** или выше (`libssl-dev`)
*   **libcurl:** (`libcurl4-openssl-dev`)

**Установка одной командой в Debian/Ubuntu:**
```bash
sudo apt-get update
sudo apt-get install build-essential libsodium-dev libssl-dev libcurl4-openssl-dev
```

### 4.2. Сборка и тестирование

1.  **Собрать все цели (библиотеку, демо, CLI, тесты):**
    ```bash
    make all
    ```

2.  **Запустить модульные тесты (ключевой шаг):**
    ```bash
    make run-tests
    ```    > 😝 **Примечание об ожидаемом поведении теста OCSP**
    >
    > Один из тестовых случаев в `test_pki_verification` намеренно использует для проверки сертификат, указывающий на недействительный OCSP-сервер. Поскольку сетевой запрос неизбежно завершится неудачей, функция `hsc_verify_user_certificate` **должна** вернуть `-4`, чтобы указать на сбой проверки статуса отзыва. Тестовый код будет утверждать, что возвращаемое значение действительно равно `-4`, тем самым доказывая, что наш механизм безопасности «отказ в доступе при сбое» работает корректно.

3.  **Запустить демонстрационную программу:**
    ```bash
    ./bin/hsc_demo
    ```

4.  **Запустить утилиту командной строки:**
    ```bash
    ./bin/hsc_cli --help
    ```

5.  **Очистить файлы сборки:**
    ```bash
    make clean
    ```

## 5. ☺️ Руководство по использованию

### 5.1. Как утилита командной строки (`hsc_cli`)

`hsc_cli` — это полнофункциональная утилита командной строки, **поддерживающая гибкий порядок параметров**, для выполнения всех основных операций криптографии и PKI.

**Пример полного рабочего процесса: Алиса шифрует файл и безопасно отправляет его Бобу**

1.  **😒 (Обе стороны) Генерируют пары мастер-ключей:**
    ```bash
    ./bin/hsc_cli gen-keypair alice
    ./bin/hsc_cli gen-keypair bob
    ```

2.  **☺️ (Обе стороны) Генерируют CSR и получают сертификаты:** (Здесь предполагается, что УЦ уже выпустил `alice.pem` и `bob.pem`)
    ```bash
    ./bin/hsc_cli gen-csr alice.key "alice@example.com"
    # (Отправьте alice.csr в УЦ, чтобы получить alice.pem)
    ```

3.  **🤨 (Алиса) Проверяет сертификат Боба:** (Предполагается, что `ca.pem` — это доверенный корневой сертификат УЦ)
    ```bash
    ./bin/hsc_cli verify-cert bob.pem --ca ca.pem --user "bob@example.com"
    ```
    > **Подсказка:** Опции со значениями (например, `--ca` и `--user`) теперь можно перечислять в любом порядке.

4.  **😑 (Алиса) Шифрует файл для Боба:**
    ```bash
    echo "This is top secret information." > secret.txt
    ./bin/hsc_cli encrypt secret.txt --to bob.pem --from alice.key
    ```
    Теперь Алиса может отправить `secret.hsc` и свой собственный сертификат `alice.pem` Бобу.

5.  **😉 (Боб) Расшифровывает файл после получения:**
    ```bash
    # Боб также может поменять местами --from и --to
    ./bin/hsc_cli decrypt secret.hsc --to bob.key --from alice.pem
    cat secret.decrypted
    ```

### 5.2. Интеграция в ваш проект в качестве библиотеки

Файл `src/main.c` является отличным примером интеграции. Ниже представлен типичный порядок вызовов API:

1.  **Глобальная инициализация:** При запуске программы вызовите `hsc_init()`.
    ```c
    #include "hsc_kernel.h"
    
    int main() {
        if (hsc_init() != 0) {
            // Обработка критической ошибки
        }
        // ... ваш код ...
        hsc_cleanup();
        return 0;
    }
    ```

2.  **Отправитель (Алиса) шифрует данные:**
    ```c
    // 1. Сгенерировать одноразовый сессионный ключ
    unsigned char session_key[HSC_SESSION_KEY_BYTES];
    randombytes_buf(session_key, sizeof(session_key));

    // 2. Зашифровать данные с помощью сессионного ключа, используя AEAD (подходит для небольших данных)
    const char* message = "Secret message";
    size_t enc_buf_size = strlen(message) + HSC_AEAD_NONCE_BYTES + HSC_AEAD_TAG_BYTES;
    unsigned char* encrypted_data = malloc(enc_buf_size);
    unsigned long long encrypted_data_len;
    hsc_aead_encrypt(encrypted_data, &encrypted_data_len, 
                     (const unsigned char*)message, strlen(message), session_key);

    // 3. Проверить сертификат получателя (Боба)
    if (hsc_verify_user_certificate(bob_cert_pem, ca_pem, "bob@example.com") != 0) {
        // Сертификат недействителен, прервать!
    }

    // 4. Извлечь открытый ключ Боба из его сертификата
    unsigned char bob_pk[HSC_MASTER_PUBLIC_KEY_BYTES];
    hsc_extract_public_key_from_cert(bob_cert_pem, bob_pk);

    // 5. Инкапсулировать сессионный ключ, используя открытый ключ Боба и закрытый ключ Алисы
    // (Предполагается, что alice_kp — это загруженный hsc_master_key_pair*)
    unsigned char encapsulated_key[...]; size_t encapsulated_key_len;
    hsc_encapsulate_session_key(encapsulated_key, &encapsulated_key_len, 
                                session_key, sizeof(session_key),
                                bob_pk, alice_kp);
    
    // 6. Отправить encrypted_data и encapsulated_key Бобу
    ```

3.  **Получатель (Боб) расшифровывает данные:**
    ```c
    // 1. Извлечь открытый ключ отправителя (Алисы) из ее сертификата
    unsigned char alice_pk[HSC_MASTER_PUBLIC_KEY_BYTES];
    hsc_extract_public_key_from_cert(alice_cert_pem, alice_pk);
    
    // 2. Декапсулировать сессионный ключ, используя открытый ключ Алисы и собственный закрытый ключ Боба
    // (Предполагается, что bob_kp — это загруженный hsc_master_key_pair*)
    unsigned char* dec_session_key = hsc_secure_alloc(HSC_SESSION_KEY_BYTES);
    if (hsc_decapsulate_session_key(dec_session_key, encapsulated_key, enc_key_len,
                                    alice_pk, bob_kp) != 0) {
        // Ошибка декапсуляции!
    }

    // 3. Расшифровать данные с помощью восстановленного сессионного ключа
    unsigned char final_message[...]; unsigned long long final_len;
    if (hsc_aead_decrypt(final_message, &final_len,
                         encrypted_data, encrypted_data_len, dec_session_key) != 0) {
        // Ошибка расшифровки! Данные были подделаны
    }

    // 4. Безопасно освободить сессионный ключ после использования
    hsc_secure_free(dec_session_key);
    ```

## 6. 😶 Глубокий анализ технической архитектуры

Ядром этого проекта является модель гибридного шифрования, которая сочетает в себе преимущества асимметричного и симметричного шифрования для достижения как безопасной, так и эффективной передачи данных.

**Схема потоков данных и отношений ключей:**

```
ОТПРАВИТЕЛЬ (АЛИСА)                                      ПОЛУЧАТЕЛЬ (БОБ)
========================================================================
[Исходные данные] -> Генерирует [Сессионный ключ]
                      |        |
(Симметричное шифрование) <--'        '-> (Асимметричная инкапсуляция) с исп.: Открытого ключа Боба, Закрытого ключа Алисы
       |                                      |
[Зашифрованные данные]               [Инкапсулированный сессионный ключ]
       |                                      |
       '----------------. .-------------------'
                        | |
                        v v
                    [Пакет для передачи]
                         |
     ==================> | Сеть/Передача файла =================>
                         |
                    [Пакет для передачи]
                        | |
             .----------' '-------------.
             |                          |
[Инкапсулированный сессионный ключ]   [Зашифрованные данные]
             |                          |
             v                          |
(Асимметричная декапсуляция) с исп.: Закрытого ключа Боба, Открытого ключа Алисы |
             |                          |
             v                          |
        [Восстановленный сессионный ключ] <-$----' (Симметричное расшифровывание)
             |
             v
        [Исходные данные]
```

## 7. 😄 Расширенная настройка: повышение безопасности с помощью переменных окружения

Чтобы адаптироваться к более мощному оборудованию и будущим требованиям безопасности без изменения кода, этот проект поддерживает **усиление** вычислительной стойкости функции производной ключа (Argon2id) через переменные окружения.

*   **`HSC_ARGON2_OPSLIMIT`**: Устанавливает количество операций (вычислительных итераций) для Argon2id.
*   **`HSC_ARGON2_MEMLIMIT`**: Устанавливает объем используемой памяти для Argon2id (в байтах).

**Важное замечание по безопасности:** Эта функция может использоваться **только для повышения параметров безопасности**. Если значения, установленные в переменных окружения, ниже встроенного минимального уровня безопасности, программа автоматически проигнорирует эти небезопасные значения и принудительно будет использовать встроенные минимальные.

** Новый пример использования:**

```bash
# Пример: Увеличить лимит операций до 10 и лимит памяти до 512 МБ.
# Примечание: HSC_ARGON2_MEMLIMIT должен быть указан в байтах.
# 512 * 1024 * 1024 = 536870912 байт.
export HSC_ARGON2_OPSLIMIT=10
export HSC_ARGON2_MEMLIMIT=536870912

# Запустите программу в оболочке, где установлены переменные окружения.
# Она автоматически будет использовать эти более сильные параметры.
./bin/hsc_cli gen-keypair my_strong_key
```

## 8. 😀 Справочник по основным API (`include/hsc_kernel.h`)

### Инициализация и очистка
| Функция | Описание |
| :--- | :--- |
| `int hsc_init()` | **(Должна вызываться первой)** Инициализирует всю библиотеку. |
| `void hsc_cleanup()` | Вызывается перед выходом из программы для освобождения глобальных ресурсов. |

### Управление ключами
| Функция | Описание |
| :--- | :--- |
| `hsc_master_key_pair* hsc_generate_master_key_pair()` | Генерирует новую пару мастер-ключей. |
| `hsc_master_key_pair* hsc_load_master_key_pair_from_private_key(...)` | Загружает закрытый ключ из файла. |
| `int hsc_save_master_key_pair(...)` | Сохраняет пару ключей в файл. |
| `void hsc_free_master_key_pair(hsc_master_key_pair** kp)` | Безопасно освобождает пару мастер-ключей. |

### PKI и сертификаты
| Функция | Описание |
| :--- | :--- |
| `int hsc_generate_csr(...)` | Генерирует запрос на подпись сертификата (CSR) в формате PEM. |
| `int hsc_verify_user_certificate(...)` | **(Ключевая)** Выполняет полную проверку сертификата (цепочка, срок действия, субъект, OCSP). |
| `int hsc_extract_public_key_from_cert(...)` | Извлекает открытый ключ из проверенного сертификата. |

### Инкапсуляция ключа (асимметричная)
| Функция | Описание |
| :--- | :--- |
| `int hsc_encapsulate_session_key(...)` | Шифрует сессионный ключ с помощью открытого ключа получателя. |
| `int hsc_decapsulate_session_key(...)` | Расшифровывает сессионный ключ с помощью закрытого ключа получателя. |

### Шифрование данных (симметричное)
| Функция | Описание |
| :--- | :--- |
| `int hsc_aead_encrypt(...)` | Выполняет аутентифицированное шифрование **небольшого блока данных** с помощью AEAD. |
| `int hsc_aead_decrypt(...)` | Расшифровывает и проверяет данные, зашифрованные `hsc_aead_encrypt`. |

### Поточное шифрование (симметричное, для больших файлов)
| Функция | Описание |
| :--- | :--- |
| `hsc_crypto_stream_state* hsc_crypto_stream_state_new_push(...)` | Создает объект состояния потока шифрования. |
| `hsc_crypto_stream_state* hsc_crypto_stream_state_new_pull(...)` | Создает объект состояния потока расшифровывания. |
| `int hsc_crypto_stream_push(...)` | Шифрует один блок данных в потоке. |
| `int hsc_crypto_stream_pull(...)` | Расшифровывает один блок данных из потока. |
| `void hsc_crypto_stream_state_free(hsc_crypto_stream_state** state)` | Освобождает объект состояния потока. |

### Безопасная память
| Функция | Описание |
| :--- | :--- |
| `void* hsc_secure_alloc(size_t size)` | Выделяет защищенный, не выгружаемый на диск блок памяти. |
| `void hsc_secure_free(void* ptr)` | Безопасно очищает и освобождает защищенную память. |


## 9. 🥳 Вклад в проект

Мы приветствуем любой вклад! Если вы нашли ошибку, у вас есть предложение по новой функции или вы хотите улучшить документацию, не стесняйтесь отправлять Pull Request или создавать Issue.

## 10. 🥺 Описание сертификатов

Этот проект использует систему сертификатов **X.509 v3** для привязки открытого ключа к идентификатору пользователя (например, `alice@example.com`), тем самым устанавливая доверие. Процесс проверки сертификата включает **проверку цепочки подписей**, **проверку срока действия**, **проверку идентификатора субъекта** и **проверку статуса отзыва (OCSP)**, с применением строгой политики «отказа в доступе при сбое».

## 11. 🥸 Лицензия - Модель двойного лицензирования

Этот проект использует модель **двойного лицензирования (Dual-License)**:

### 1. GNU Affero General Public License v3.0 (AGPLv3)
Подходит для проектов с открытым исходным кодом, академических исследований и личного обучения. Требует, чтобы любые модифицированные производные работы или работы, предоставляемые как сетевой сервис, также публиковали свой полный исходный код под лицензией AGPLv3.

### 2. Коммерческая лицензия
Подходит для любых коммерческих приложений, продуктов или услуг с закрытым исходным кодом. Если вы не хотите быть связанными условиями лицензии AGPLv3, вы должны приобрести коммерческую лицензию.

**Для получения коммерческой лицензии, пожалуйста, свяжитесь с: `eldric520lol@gmail.com`**